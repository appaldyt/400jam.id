<!-- Headline Section -->
<section class="mb-6">
    <h2 class="text-2xl font-bold mb-4">Top Story</h2>
    <div class="bg-white shadow-lg rounded-lg p-4">
        <img src="<%= headline.image %>" alt="<%= headline.title %>" class="w-full h-56 object-cover rounded-lg mb-3">
        <h3 class="text-xl font-semibold mb-2"><%= headline.title %></h3>
        <p class="text-gray-600"><%= headline.summary %></p>
    </div>
    <div class="bg-white shadow-lg rounded-lg p-4">
        <img src="<%= headline.image %>" alt="<%= headline.title %>" class="w-full h-56 object-cover rounded-lg mb-3">
        <h3 class="text-xl font-semibold mb-2"><%= headline.title %></h3>
        <p class="text-gray-600"><%= headline.summary %></p>
    </div>
    <div class="bg-white shadow-lg rounded-lg p-4">
        <img src="<%= headline.image %>" alt="<%= headline.title %>" class="w-full h-56 object-cover rounded-lg mb-3">
        <h3 class="text-xl font-semibold mb-2"><%= headline.title %></h3>
        <p class="text-gray-600"><%= headline.summary %></p>
    </div>
</section>